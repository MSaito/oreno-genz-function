#include <stdint.h>
#include <cmath>

// 森 修論 いや無理だわこれ
double cornerpeak_expect(int dim, double alpha[], double *)
{
    uint64_t max = 2;
    max = max << s;
    double fact = 1.0;
    for (int i = 0; i <
    for (uint64_t x = 0; x < max; x++) {

    }
}
